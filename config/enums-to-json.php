<?php

// config for Creekmore108/LaravelEnumsToJson
return [

    'enum_locations' => [
        'app_path()',
    ],

    // The disk to use for storing the generated JSON files.
    'disk' => 'public',

    'path' => '/shared', // The folder to store the generated JSON files.

    'format_label' => true,

];
